game:
  library_url: http://library-service/gameslist
eureka:
  client:
    enabled: false
spring:
  cloud:
    config:
      failfast: false
  profiles:
    active: test
stubrunner:
  idsToServiceIds:
    games: library-service
